# Install the following dependencies
# Using pip to install them on a Mac by the following command line: 
# pip install -r requirements.txt

PyMuPDF==1.22.1
numpy==1.23.5
scikit-learn==1.2.2
tensorflow # >=2.0.0
tensorflow_hub # ==0.13.0
openai==0.27.4
gradio==3.27.0
langchain-serve # >=0.0.19

# if you keel receive error message about dependencies conflict for tensorflow-macos, tensorboar, and jina
# use this pip comand line to avoid the strict version for each dependencies
# pip install tensorflow-macos tensorboard jina